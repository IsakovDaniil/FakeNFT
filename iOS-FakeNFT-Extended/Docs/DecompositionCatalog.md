Брюковкин Владимир Викторович 

- Когорта: 31  
- Группа: 3  
- Эпик: Каталог  

Ссылка на доску: https://github.com/users/IsakovDaniil/projects/3

# Декомпозиция эпика Каталог

## Модуль 1: Экран каталога (список коллекций)

### Верстка / UI

- **Экран каталога:**  
  `NavigationStack` с `List` коллекций, кнопка сортировки (иконка ☰) в navigation bar  
  (est: 1,5 часа; fact: 2 часов)

- **Ячейка коллекции:**  
  Горизонтальный ряд из 3 изображений (Kingfisher), название коллекции, количество NFT в скобках  
  (est: 2 часа; fact: 5 часов)

- **Action Sheet сортировки:**  
  Заголовок "Сортировка", варианты: "По названию", "По количеству NFT", кнопка "Закрыть"  
  (est: 1 час; fact: 1 час)

- **Индикатор загрузки:**  
  `ProgressView` по центру экрана при загрузке данных  
  (est: 0,5 часа; fact: 0,5 часов)

- **Состояние ошибки:**  
  Алерт "Не удалось получить данные" с кнопками "Отмена" / "Повторить"  
  (est: 0,5 часа; fact: 0,5 часов)

### Логика

- **CatalogViewModel:**  
  Хранение состояния экрана (loading, loaded, error), списка коллекций, типа сортировки  
  (est: 1,5 часа; fact: 2 часа)

- **Модель данных:**  
  `NFTCollection` с полями: id, name, cover, nfts, description, author  
  (est: 0,5 часа; fact: 1 часа)

- **Сортировка списка:**  
  По названию, по количеству NFT, сохранение выбора в `@AppStorage`  
  (est: 1 час; fact: 2 часа)

- **Загрузка коллекций:**  
  Вызов метода сервиса, обновление состояния при успехе/ошибке  
  (est: 1 час; fact: 1 час)

- **Навигация:**  
  Переход на экран коллекции при тапе на ячейку  
  (est: 0,5 часа; fact: 1 час)

### Работа с данными

- **Получение списка коллекций:**  
  Создать GET запрос `/api/v1/collections`  
  Маппинг ответа в модель `NFTCollection`  
  (est: 1,5 часа; fact: 2 часа)

- **CollectionService (actor):**  
  Метод `fetchCollections() async throws -> [NFTCollection]`  
  Обработка ошибок сети  
  (est: 1 час; fact: 1 час)

**Итого Модуль 1:** est: ~12,5 часов; fact: 19 часов

---

## Модуль 2: Экран коллекции NFT

### Верстка / UI

- **Обложка коллекции:**  
  Большое изображение сверху (Kingfisher), скруглённые углы снизу, кнопка "назад" поверх изображения  
  (est: 1,5 часа; fact: 2 часа)

- **Информация о коллекции:**  
  Название (заголовок), имя автора (кликабельная ссылка синим цветом), описание (многострочный текст)  
  (est: 1 час; fact: 1 час)

- **Сетка NFT:**  
  `LazyVGrid` с 3 колонками, вертикальный скролл  
  (est: 1 час; fact: 1 час)

- **Ячейка NFT:**  
  Изображение (Kingfisher), кнопка лайка (сердце) в углу, название, рейтинг (звёзды), цена в ETH, кнопка корзины  
  (est: 2,5 часа; fact: 2 часа)

- **WebView для сайта автора:**  
  `WKWebView` через `UIViewRepresentable`, navigation bar с кнопкой назад  
  (est: 1 час; fact: 5 часов)

- **Индикатор загрузки:**  
  `ProgressView` при загрузке NFT коллекции  
  (est: 0,5 часа; fact: 0.5 часов)

### Логика

- **CollectionDetailViewModel:**  
  Хранение состояния экрана, данных коллекции, списка NFT, избранных ID, ID в корзине  
  (est: 1,5 часа; fact: 2 часов)

- **Модель данных:**  
  `NFT` с полями: id, name, images, rating, price, author, description  
  (est: 0,5 часа; fact: 1 час)

- **Загрузка NFT коллекции:**  
  Параллельная загрузка NFT по массиву ID из коллекции (async let или withTaskGroup)  
  (est: 2 часа; fact: 3 часа)

- **Определение состояния лайка/корзины:**  
  Проверка наличия NFT ID в массивах likes и cart  
  (est: 1 час; fact: 1 часов)

- **Навигация:**  
  Переход на WebView при тапе на автора, переход на экран NFT при тапе на ячейку  
  (est: 1 час; fact: 1 час)

### Работа с данными

- **Получение NFT по ID:**  
  Создать GET запрос `/api/v1/nft/{id}`  
  Маппинг ответа в модель `NFT`  
  (est: 1 час; fact: 1 час)

- **NftService (actor):**  
  Метод `fetchNft(id:) async throws -> NFT`  
  Метод `fetchNfts(ids:) async throws -> [NFT]` для batch загрузки  
  (est: 1,5 часа; fact: 1 час)

- **Получение профиля (для likes):**  
  GET запрос `/api/v1/profile/1` для получения массива likes  
  (est: 1 час; fact: 1 час)

- **Получение заказа (для корзины):**  
  GET запрос `/api/v1/orders/1` для получения массива nfts в корзине  
  (est: 1 час; fact: 1 часов)

**Итого Модуль 2:** est: ~17 часов; fact: 23.5 часов

---

## Модуль 3: Интерактивность и интеграция

### Верстка / UI

- **Состояния кнопки лайка:**  
  Пустое/заполненное сердце, анимация при нажатии  
  (est: 1 час; fact: 1 час)

- **Состояния кнопки корзины:**  
  Иконка корзины / крестик (если в корзине), анимация при нажатии  
  (est: 1 час; fact: 1 час)

- **Состояние ошибки на экране коллекции:**  
  Алерт "Не удалось получить данные" с кнопками "Отмена" / "Повторить"  
  (est: 0,5 часа; fact: 1 час)

### Логика

- **Добавление/удаление из избранного:**  
  Формирование нового массива likes, вызов PUT, обновление UI  
  (est: 1,5 часа; fact: 2 часа)

- **Добавление/удаление из корзины:**  
  Формирование нового массива nfts, вызов PUT, обновление UI  
  (est: 1,5 часа; fact: 2 часа)

- **Обработка ошибок сети:**  
  Retry логика, отображение алертов, восстановление состояния при ошибке  
  (est: 1,5 часа; fact: 2 часа)

- **Навигация на экран NFT:**  
  Интеграция с NftDetailBridgeView (уже есть в скелете проекта)  
  (est: 1 час; fact: 2 часа)

### Работа с данными

- **Обновление избранного:**  
  PUT запрос `/api/v1/profile/1` с телом `likes=[id1,id2,...]`  
  Content-Type: application/x-www-form-urlencoded  
  (est: 1,5 часа; fact: 1 час)

- **Обновление корзины:**  
  PUT запрос `/api/v1/orders/1` с телом `nfts=[id1,id2,...]`  
  Content-Type: application/x-www-form-urlencoded  
  (est: 1,5 часа; fact: 1 час)

- **ProfileService (actor):**  
  Методы `fetchProfile()`, `updateLikes(ids:)`  
  (est: 1 час; fact: 0,5 часов)

- **OrderService (actor):**  
  Методы `fetchOrder()`, `updateOrder(nftIds:)`  
  (est: 1 час; fact: 0,5 часов)

### Тестирование и полировка

- **Тестирование всех сценариев:**  
  Загрузка, сортировка, лайки, корзина, навигация, ошибки  
  (est: 2 часа; fact: 2 часа)

- **Исправление багов:**  
  Фиксы UI, логики, edge cases  
  (est: 2 часа; fact: 1 час)

- **Полировка UX:**  
  Плавность анимаций, корректность состояний, сообщения об ошибках  
  (est: 1 час; fact: 1 час)

**Итого Модуль 3:** est: ~17,5 часов; fact: 18 часов


